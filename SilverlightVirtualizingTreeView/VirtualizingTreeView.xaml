<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:SilverlightVirtualizingTreeView" x:Class="SilverlightVirtualizingTreeView.VirtualizingTreeView"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Background="White">
        <ListBox x:Name="InnerListBox" HorizontalAlignment="Stretch" Height="Auto" VerticalAlignment="Stretch" Width="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:VirtualizingTreeViewItem
                        IsExpanded="{Binding Path=IsExpanded, Mode=TwoWay}" 
                        ExpanderVisibility="{Binding Path=ExpanderVisibility}" 
                        Text="{Binding Path=Text}" 
                        Indent="{Binding Path=Indent}"
                        HorizontalAlignment="Stretch" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
